const NormalButton = (props) => {
    return (
        <div>
            <p>{props.title}</p>
            <p>{props.title}</p>
            <p>{props.title}</p>
            <p>{props.title}</p>
            <p>{props.title}</p>
        </div>
    );
}

export default NormalButton;
